# Pinned Python stack for the Stack-in-a-Box reference platform.
# Read by scripts/setup/04_python_venv.sh (pip install -r requirements.txt).
#
# Versions are pinned for reproducible installs. Sourced from a proven
# working set; revisit after the first real EC2 install (Plan 3) confirms
# they resolve cleanly on Ubuntu 24.04 ARM.
dlt[duckdb]==1.26.0
dbt-core==1.11.9
dbt-duckdb==1.10.1
python-ulid==3.1.0
duckdb>=1.1.0      # let dbt-duckdb resolve the exact build; minimum constraint only
pyyaml>=6.0
requests>=2.31     # source-health checks + dlt SODA pulls
